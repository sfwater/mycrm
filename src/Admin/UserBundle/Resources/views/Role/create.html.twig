{% extends 'AdminConsoleBundle::page_edit.html.twig' %}
{% block formContent %}
  <div class="form-group">
    {{ form_label(form.name) }}
    {{ form_widget(form.name) }}
  </div>
  <div class="form-group">
    {{ form_label(form.role) }}
    {{ form_widget(form.role) }}
  </div>
  <div class="form-group">
    {{ form_row(form.mask) }}
  </div>
{% endblock %}