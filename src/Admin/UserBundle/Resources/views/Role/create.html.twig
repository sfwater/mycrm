{% extends 'AdminConsoleBundle::page_edit.html.twig' %}
{% block formContent %}
  <div class="form-group">
    {{ form_row(form.name) }}
  </div>
  <div class="form-group">
    {{ form_row(form.role) }}
  </div>
  <div class="form-group">
    {{ form_row(form.mask) }}
  </div>
{% endblock %}